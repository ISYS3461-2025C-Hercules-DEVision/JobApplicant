# ===============================
# Application Info
# ===============================
spring.application.name=subscription-service
server.port=8086

spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# ===============================
# MongoDB
#   - Reads from env var MONGODB_URI_SUBSCRIPTION
# ===============================
spring.data.mongodb.uri=${MONGODB_URI_SUBSCRIPTION}

# ===============================
# Swagger / OpenAPI (optional)
# ===============================
springdoc.paths-to-match=/api/**
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# ===============================
# Eureka Client (Service Discovery)
# ===============================
eureka.client.service-url.defaultZone=${EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka/}
eureka.instance.prefer-ip-address=true

# ===============================
# Stripe Configuration
#   - Reads from env vars STRIPE_SECRET_KEY and STRIPE_WEBHOOK_SECRET
# ===============================
stripe:
  secret-key: sk_test_xxxxxxxxx
  webhook-secret: whsec_xxxxxxxxx
  price-usd: 1000   # 10 USD = 1000 cents
