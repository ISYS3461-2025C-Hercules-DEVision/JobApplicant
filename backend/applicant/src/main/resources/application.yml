server:
  port: ${PORT:8080}

spring:
  application:
    name: applicant-service

  data:
    mongodb:
      uri: mongodb+srv://ungxuandat11092001_db_user:8ZaEZXpeGQ6Zpxbq@applicant.44pbxjf.mongodb.net/applicant-db?retryWrites=true&w=majority&appName=Applicant

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
