package com.devision.application.dto.external.request;

public class UploadCvRequest {
    // Placeholder DTO for documentation purpose (multipart is handled in controller)
    // Keep here if your team wants consistent structure.
}
